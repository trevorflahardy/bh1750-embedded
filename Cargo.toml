[package]
name = "bh1750-embedded"
version = "0.1.0"
edition = "2021"
authors = ["Trevor Flahardy"]
license = "MIT"
description = "no_std embedded-hal driver for the BH1750/BH1750FVI ambient light sensor (with optional async support)."
repository = "https://github.com/trevorflahardy/bh1750-embedded"
keywords = ["embedded-hal", "i2c", "bh1750", "light", "no-std"]
categories = ["embedded", "hardware-support", "no-std"]

[features]
# Enable async I2C + delay support via embedded-hal-async.
async = ["dep:embedded-hal-async"]

[dependencies]
embedded-hal = "1.0.0"
embedded-hal-async = { version = "1.0.0", optional = true }

[dev-dependencies]
embedded-hal-mock = { version = "0.11", default-features = false, features = ["eh1"] }
tokio = { version = "1", features = ["macros", "rt"] }

[profile.release]
lto = true
